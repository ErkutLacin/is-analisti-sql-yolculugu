# 07 — WHERE, GROUP BY ve HAVING Alıştırması

Bu alıştırma, Medium’daki  
**“Oracle SQL’de Filtreleme ve Gruplama — WHERE Operatörleri, GROUP BY ve HAVING”**  
yazısına bağlıdır.

## 🎯 Amaç
Bu çalışmada:
- WHERE ile satırları filtreleme
- GROUP BY ile verileri kategorilere ayırma
- HAVING ile gruplar üzerinde koşul uygulama

konularını pratik ediyoruz.

Bu bölüm, SQL’in raporlama ve veri analizi tarafına geçiş noktasıdır.

## 🧪 Nasıl Çalıştırılır?
Önce `musteri_islemleri` tablosunun oluşturulduğu veri setini çalıştırın.  
Ardından sorguları adım adım deneyin ve sonuçları inceleyin.
